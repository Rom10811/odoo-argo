apiVersion: v1
kind: Service
metadata:
  name: odoo
  labels:
    app: odoo
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8069
    protocol: TCP
    name: http
  selector:
    app: odoo
